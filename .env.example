DIRECTUS_URL=http://localhost:8055
DIRECTUS_TOKEN=

#CONTACT_EMAIL=hola@centavos.mx

# Resend (newsletter + welcome email)
RESEND_API_KEY=
RESEND_FROM="Centavo <hola@centavos.mx>"
RESEND_REPLY_TO=hola@centavos.mx

# Claude (article generation — /api/articles/generate)
ANTHROPIC_API_KEY=
# Shared secret required in `x-cron-article` header (Directus cron flow).
# Also accepted as `Authorization: Bearer <…>` for manual curl/scripts.
ARTICLE_GENERATOR_SECRET=
